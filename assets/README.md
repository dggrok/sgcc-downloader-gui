# 图标资源说明

## 目标文件
- Windows 打包优先读取 `assets/app.ico`

## 建议规格
- 格式：`.ico`
- 尺寸：至少包含 `256x256`
- 风格：简洁、扁平、识别度高，适合桌面快捷方式和资源管理器显示

## 当前状态
- 项目已在打包配置中预留图标接入位
- 如果 `assets/app.ico` 不存在，仍可正常构建，只是 exe 会使用默认图标

## 推荐命名
```text
assets/app.ico
```
